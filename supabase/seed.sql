-- Intentionally empty.
--
-- Owner accounts are never seed data: create the single local owner with
-- `pnpm bootstrap:user`. Keeping this tracked file makes `supabase db reset`
-- deterministic while signups remain disabled.

